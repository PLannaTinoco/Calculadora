#include <stdio.h>
#include <stdbool.h>



int main()
{
  int op = 0;
  bool erro;
  float a;
  printf(" Calculadora \n Fale um numero: ");
  scanf("%f", &a);

  do{
  do{

    printf(" \n\n 0-sair \n 1-adicao \n 2-subtracao \n 3-multiplicacao \n 4-divisao");
    printf("\n Qual opção? ");
    scanf("%i", &op);
    erro = op<0 || op>4;
    if(erro)puts("\n\a valor inválido ");
    if(op==0){printf("Obrigado");
    return 0;}


   } while(erro);



  float b;
  printf("\n Fale um numero: ");
  scanf("%f", &b);
  float resultado = 0;

  switch(op)
  {
  case 0:
  break;
  case 1:
    resultado = (a + b);
    printf("%.f + %.f = %.f", a, b, resultado);
    a = resultado;
    break;
  case 2:
  resultado = (a - b);
  printf("%.f - %.f = %.f", a, b, resultado);
    a = resultado;
    break;
  case 3:
  resultado = (a * b);
  printf("%.f * %.f = %.f", a , b, resultado);
  a = resultado;
    break;
  case 4:
  resultado = (a / b);
  printf("%.f / %.f = %.f", a, b, resultado);
  a = resultado;
    break;
    }

  //printf("%.f", a);
  }while(op!=0);

    return 0;
}
